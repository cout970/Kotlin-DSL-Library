package kdl.internal.gui

import net.minecraft.inventory.Inventory
import net.minecraft.screen.slot.Slot

class KDLSlot(inventory: Inventory, index: Int, x: Int, y: Int) : Slot(inventory, index, x, y)